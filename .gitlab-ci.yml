image: docker:latest

stages:
  - проверка
  - завершение

проверка:
  stage: проверка
  script:
    - echo "=== ПРОВЕРКА DOCKER ==="
    - docker --version
    - echo "Docker доступен"

завершение:
  stage: завершение
  script:
    - echo "=== ЗАВЕРШЕНИЕ ==="
    - echo "Pipeline выполнен успешно!"
    - echo "Все работает корректно"
